export async function getAvailableModels(): Promise<string[]> {
  // This function is not actually used, as the models are hardcoded in the backend.
  // However, we need to provide a dummy implementation to satisfy the import.
  return []
}
